<nav class="pagination" role="navigation">
    {{#if prev}}
        <a class="page-prev" href="{{page_url prev}}">上一页>></a>
    {{/if}}
    <span class="page-number">{{page}} / {{pages}}</span>
    {{#if next}}
        <a class="page-next" href="{{page_url next}}">下一页>></a>
    {{/if}}
</nav>